import { ILocale } from "../Locale";

/** strings (English) */
export const LocaleEn: ILocale = {
    Auth: {
        SectionTitle: "Please log in",
        PleaseLogin: "You must log in with an account that can post to SharePoint list.",
        DomainPlaceHolder: "Domain",
        LoginButton: "Log in",
        LogoutButton: "Log out"
    }
}